;;; dap-config --- Custom config for dap-mode -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package dap-mode
  :defer t
  :custom
  ;; (dap-mode 1)
  ;; (dap-ui-mode 1)
  ;; (dap-tooltip-mode 1)
  ;; (tooltip-mode 1)
  ;; (dap-ui-controls-mode 1)
  (dap-auto-show-output nil))

(provide 'dap-condig)
;;; dap.el ends here
